import { styled } from "styled-components";

export const StyledGridComponentes = styled.div`

margin: 10vh 0 ;
display: grid;
gap: 5rem;
  grid-auto-rows: auto;
  grid-template-columns: repeat(auto-fill, minmax(20em, 1fr));

`